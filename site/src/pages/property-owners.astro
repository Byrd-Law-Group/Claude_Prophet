---
import { getEntry, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ContactForm from '../components/ContactForm.astro';
import { marked } from '../lib/inline-md';

const entry = await getEntry('pages', 'property-owners');
if (!entry) throw new Error('Missing content: src/content/pages/property-owners.md');
const { title, description, afterFormNote } = entry.data;
const { Content } = await render(entry);
---

<BaseLayout title={title} description={description}>
  <section class="section">
    <div class="container prose">
      <Content />

      <ContactForm variant="owners" formName="Property owner inquiry" />

      {
        afterFormNote && (
          <div class="card" style="margin-top:2rem;">
            <p set:html={marked(afterFormNote)} />
          </div>
        )
      }
    </div>
  </section>
</BaseLayout>
