---
import { site, nav } from '../siteData';

// Mark the current page for aria-current styling.
const current = Astro.url.pathname.replace(/\/+$/, '') || '/';
const isActive = (href: string) =>
  (href.replace(/\/+$/, '') || '/') === current;
---

<header class="site-header">
  <div class="container">
    <a class="brand" href="/" aria-label="6170 Properties — home">
      <img class="brand-logo" src="/6170-primary-cream.svg" alt="" width="66" height="64" />
    </a>
    <nav class="site-nav" aria-label="Primary">
      <ul>
        {
          nav.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isActive(item.href) ? 'page' : undefined}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
